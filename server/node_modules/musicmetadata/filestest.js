var fs = require('fs');
var musicmetadata = require('./lib');

['/Users/leetreveil/Downloads/Game.of.Thrones.S03E06.HDTV.x264-2HD.mp4'].forEach(function (filename) {
  var parser = new musicmetadata(fs.createReadStream(filename));
  parser.on('metadata', function (result) {
    console.log(result);
  })
});